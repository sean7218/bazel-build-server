import Foundation
import Utils

public struct NetworkStack {
    public let host: String
    public init(host: String) {
        self.host = host
    }

    public func request(url: String) throws {
        fatalError("todo")
    }
}
